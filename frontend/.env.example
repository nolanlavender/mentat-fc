# Vite exposes any VITE_-prefixed variable to client-side code (it's bundled into
# the JS the browser downloads). Never put API-Football/Odds/Groq keys here — those
# stay server-side in backend/.env. The frontend only needs to know where the API is.
VITE_API_BASE_URL=http://localhost:4000
