# Used by docker-compose.yml to configure the local Postgres container.
# Copy to .env (gitignored) and adjust if you like — defaults work out of the box.
POSTGRES_USER=mentat
POSTGRES_PASSWORD=mentat_dev_password
POSTGRES_DB=mentat_fc
