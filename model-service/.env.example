# Model service writes predictions here — same Postgres instance the backend reads from
DATABASE_URL=postgresql://mentat:mentat_dev_password@localhost:5432/mentat_fc

# Only needed once the model pulls live upcoming fixtures (Phase 5+) rather than
# training purely on historical football-data.co.uk CSVs
API_FOOTBALL_KEY=your_api_football_key_here
